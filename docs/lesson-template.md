# Lesson Template

Use this template for each new lesson or runnable example. Replace bracketed
text with concrete values before publishing.

## Lesson Title

One sentence describing what the learner will build or understand.

## Goal

- Explain the concept this lesson covers.
- State what the learner should know by the end.

## Prerequisites

- Required toolchains with versions when relevant
- Required package managers or system libraries
- Browser requirements if the example depends on browser features

Example:

```text
- Rust 1.80+
- wasm-pack 0.13+
- Node.js 20+
- A modern Chromium- or Firefox-based browser
```

## Files To Know

- `path/to/file`: what it does
- `path/to/other-file`: why the learner should inspect it

## Install

Document dependency installation only.

```bash
# Example only; replace with the real command set
<install command>
```

## Build

Document the command that produces the WebAssembly module or related assets.

```bash
# Example only; replace with the real command set
<build command>
```

## Run

Document how to serve or execute the example locally.

```bash
# Example only; replace with the real command set
<run command>
```

## Expected Result

- Describe what should appear in the terminal, browser, or output files.
- Include screenshots or copied output when that will remove ambiguity.

## How It Works

- Briefly explain the important moving parts.
- Keep the explanation aligned with the files and commands above.

## Troubleshooting

- `command not found`: explain which prerequisite is missing
- browser cannot load `.wasm`: explain the required local server setup
- build succeeds but output is stale: explain how to clean and rebuild

## Next Step

- Link to the next lesson
- Link to related examples or reference material
